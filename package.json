{
  "name": "cf-sample-server",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "rimraf dist && tsc",
    "start": "node ./dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.3",
    "rimraf": "^6.1.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}